void main() {
  String? nomeCompleto = null;
  if(nomeCompleto != null){
  print(nomeCompleto);
  }

  //final
  //const

  //use final sem moderação, torne imutaveis
  final nomeCompletoFinal = 'alysson hendryk';

  const nomeCompletoConst = 'alysson Hendryk';

  //variaveis FINAIS: nao podem ser alteradas depois de inicializadas, são definidas do programa em tempo de execução(runtime)

  //variaveis CONST: Nao podem ser alteradas depois de inicializadas, sao definidas no programa em tempo de compilação
  //nao podem receber valores de outras variaveis a nao ser que essas variaveis forem da mesma classe

  //USE SEM MODERAÇÂO
}